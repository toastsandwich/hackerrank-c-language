#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
	
    int n,i,var,sum=0;
    scanf("%d", &n);
    
    while (i < 6){
        
        var = n % 10;
        
        sum = sum + var;
        
        n = n / 10;
        
        i++;
    }
    
    printf("%d",sum);
    
    
    return 0;
}